# ===============================
# 🔧 CONFIGURAÇÃO DO SERVIDOR
# ===============================
server.port=8080
spring.application.name=SistemaAutomotivo

# ===============================
# 💾 CONFIGURAÇÃO DO BANCO DE DADOS MYSQL
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/sistema_automotivo?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=290820


# Driver JDBC do MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# 🧠 CONFIGURAÇÃO DO JPA / HIBERNATE
# ===============================
# Atualiza automaticamente o schema conforme as entidades
spring.jpa.hibernate.ddl-auto=none
# Dialeto do MySQL para Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Mostrar SQL gerado no console (opcional, útil para depuração)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# 🪶 LOGS (opcional)
# ===============================
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

